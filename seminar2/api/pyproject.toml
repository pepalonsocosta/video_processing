[project]
name = "seminar2-gateway"
version = "0.1.0"
description = "Gateway for Seminar2 MPEG4 endpoints"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "httpx>=0.27.0",
]

